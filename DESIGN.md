# 创作工具箱界面规范

面向设计、剪辑和音乐创作者的本地桌面工具。优先让用户找到工具、看懂输入和确认操作结果。

## 视觉基础

- 沿用森林绿识别：主要操作 #355e46，主文字 #233a34，辅助文字 #596b61，页面 #f4f6f3，内容卡片 #ffffff。
- 字体使用平台中文字体；字体预览区保持用户选择的真实字体。正文 13px、区域标题 16px、页面标题 29px。
- 容器圆角 14px、按钮 7px、输入 6px；用间距和细边框组织内容。
- 同类操作采用相同层级。工具行中的“打开”为主操作，“收藏”为次操作；配色页突出悬浮使用。
- 普通输入框、按钮及侧栏提供可见键盘焦点；不要用当前选中状态代替焦点状态。

## 操作与状态

- 输入字段具有持续可见的标签；占位文字只承担示例作用。辅助技术可读名称应描述用途。
- 配色常用操作放在主工具栏，交换文件和代码的操作集中于“导入 / 导出”。
- 未进入颜色编辑时禁用“取消编辑”；进入编辑后聚焦颜色名称。
- 字体操作随选择数量启用：批量操作至少一项，详情一项，对照 2–4 项且均在本机可用。
- 换算输入显示当前单位，方向切换后单位随之更新。
- 状态与错误应说明结果；保存失败不得显示成功。保护状态不能暗示已经确认其他软件保存成功。

## 验证

使用原生 Windows Qt、隔离数据在 1180×800 与 1020×720 两种逻辑窗口尺寸检查。色卡名称、输入标签和关键操作不得相互遮挡。大字体预览允许裁切并通过对照页查看。

参考 [awesome-design-md 的 Linear 分析](https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md) 中克制的强调色和分层操作原则；这是第三方分析。本工具延续自身绿色视觉，并非 Linear 官方设计系统实现。

## 图片素材库（0.5.0）

采用左侧列表、右侧预览与固定字段标签；详情垂直滚动，保存 / 置顶 / 回收站操作固定在底部。长原始路径放入悬停提示，避免撑宽窗口。导入为主要入口；备份与恢复为次级动作。未选素材时禁用相关操作，正在执行任务时锁定库内编辑并提供进度与取消。

默认先收集到待整理，不强制填写标签。移除明确写作「移到回收站」，恢复单独命名，不混用删除。错误说明具体文件，任务结束后保留成功项与失败原因。
